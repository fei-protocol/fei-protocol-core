// This config lists all of the addresses that should be "safe" in the PCVGuardian.

// Deprecated: SafeAddresses are deprecated as the PCVGuardian no longer has any roles or permissions
export const SafeAddressesConfig = ['feiDAOTimelock', 'simpleFeiDaiPSM'];
